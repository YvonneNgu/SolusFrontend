<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Add necessary permissions for LiveKit voice assistant -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Allow drawing overlays on top of other apps (required for system-wide UI) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Allow running foreground services (keeps voice assistant alive in background) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- For Android 9+ foreground services types -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Wake lock to keep CPU active while listening for wake word -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Auto-start after boot (optional - if you want voice assistant to start automatically) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SolusFrontend"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Screen Capture Permission Activity -->
        <activity
            android:name=".services.overlay.ScreenCaptureActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="false" />
            
        <service
            android:name=".services.overlay.VoiceAssistantOverlayService"
            android:foregroundServiceType="microphone"
            android:enabled="true"
            android:exported="false">
            <!--
            foregroundServiceType="microphone" tells Android this services uses the microphone
            This is required for Android 10+ when using microphone in foreground services
            -->
        </service>


    </application>

</manifest>